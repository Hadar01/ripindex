/target
# index directories, wherever a test or demo left one
.ripindex/
# generated demo assets
/demo/*.gif

# Screen-recorder intermediates. The GIF itself is committed; the multi-megabyte
# project archives and frame dumps that produce it are not.
docs/img/*.zip
docs/img/frames/
docs/img/*.mp4
docs/img/*.mkv
docs/img/*.webm
Project.zip
